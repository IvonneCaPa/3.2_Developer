<div class="container mx-auto px-4 py-8">
    <a href="<?= $this->baseUrl() ?>/tasks" class="text-blue-600 hover:text-blue-900 mb-4 inline-block">← Back to tasks</a>
    
    <?php if ($this->task): ?>
    <div class="bg-white shadow-md rounded-lg p-6">
        <div class="flex justify-between items-center mb-6">
            <h1 class="text-2xl font-bold"><?= $this->task->title ?></h1>
            <div class="space-x-2">
                <a href="<?= $this->baseUrl() ?>/tasks/edit/id/<?= $this->task->id ?>" 
                   class="inline-flex items-center px-3 py-2 border border-transparent text-sm leading-4 font-medium rounded-md text-white bg-blue-600 hover:bg-blue-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-blue-500">
                    Edit
                </a>
                <a href="<?= $this->baseUrl() ?>/tasks/delete/id/<?= $this->task->id ?>" 
                   class="inline-flex items-center px-3 py-2 border border-transparent text-sm leading-4 font-medium rounded-md text-white bg-red-600 hover:bg-red-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-red-500">
                    Delete
                </a>
            </div>
        </div>
        
        <div class="grid grid-cols-1 md:grid-cols-2 gap-4 mb-6">
            <div>
                <p class="text-sm text-gray-600">User</p>
                <p class="font-medium"><?= $this->task->user ?></p>
            </div>
            <div>
                <p class="text-sm text-gray-600">Status</p>
                <span class="inline-block px-3 py-1 font-semibold rounded-full 
                    <?php 
                    switch($this->task->status) {
                        case 'pending':
                            echo 'text-yellow-900 bg-yellow-200';
                            break;
                        case 'in_progress':
                            echo 'text-blue-900 bg-blue-200';
                            break;
                        case 'done':
                            echo 'text-green-900 bg-green-200';
                            break;
                        default:
                            echo 'text-gray-900 bg-gray-200';
                    }
                    ?>">
                    <?= ucfirst(str_replace('_', ' ', $this->task->status)) ?>
                </span>
            </div>
            <div>
                <p class="text-sm text-gray-600">Start Time</p>
                <p class="font-medium"><?= $this->task->startTime ?></p>
            </div>
            <div>
                <p class="text-sm text-gray-600">End Time</p>
                <p class="font-medium"><?= $this->task->endTime ? $this->task->endTime : 'Not completed' ?></p>
            </div>
        </div>
        
        <div class="mb-6">
            <p class="text-sm text-gray-600 mb-2">Description</p>
            <p class="bg-gray-50 p-4 rounded"><?= $this->task->description ?></p>
        </div>
    </div>
    <?php else: ?>
    <div class="bg-red-100 border border-red-400 text-red-700 px-4 py-3 rounded">
        Task not found
    </div>
    <?php endif; ?>
</div>